.page-container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: #FAF6EE;
}

.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 32rpx;
  padding-top: 80rpx;
  background: #FDFBF7;
  border-bottom: 2rpx solid #F3EBDD;
}

.back-btn {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 28rpx;
  font-weight: 700;
  color: #D97D38;
  background: none;
  border: none;
  padding: 0;
  line-height: 1.5;
}

.header-title {
  font-size: 40rpx;
  font-weight: 700;
  color: #292524;
}

.add-btn {
  padding: 16rpx 24rpx;
  background: #E8924A;
  color: #FFFFFF;
  border-radius: 20rpx;
  font-size: 24rpx;
  font-weight: 700;
  display: flex;
  align-items: center;
  gap: 8rpx;
  line-height: 1.5;
}

.scroll-area {
  flex: 1;
  padding: 32rpx;
  padding-bottom: 172rpx;
  padding-bottom: calc(172rpx + constant(safe-area-inset-bottom));
  padding-bottom: calc(172rpx + env(safe-area-inset-bottom));
}

.med-item {
  background: #FDFBF7;
  border-radius: 28rpx;
  padding: 36rpx;
  border: 2rpx solid #F3EBDD;
  display: flex;
  align-items: flex-start;
  gap: 28rpx;
  margin-bottom: 20rpx;
}

.med-item-left {
  width: 88rpx;
  height: 88rpx;
  border-radius: 20rpx;
  background: rgba(240, 168, 104, 0.2);
  color: #D97D38;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.med-icon {
  font-size: 52rpx;
}

.med-item-body {
  flex: 1;
}

.med-name {
  font-size: 36rpx;
  font-weight: 700;
  color: #292524;
  display: block;
  margin-bottom: 8rpx;
}

.med-dose {
  font-size: 28rpx;
  color: #78716C;
  display: block;
}

.med-time {
  font-size: 26rpx;
  color: #D97D38;
  display: block;
  margin-top: 8rpx;
}

.empty-state {
  text-align: center;
  padding: 120rpx 40rpx;
}

.empty-icon {
  font-size: 120rpx;
  display: block;
  margin-bottom: 24rpx;
}

.empty-text {
  font-size: 28rpx;
  color: #A8A29E;
}

.bottom-nav {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  height: 132rpx;
  height: calc(132rpx + constant(safe-area-inset-bottom));
  height: calc(132rpx + env(safe-area-inset-bottom));
  min-height: 132rpx;
  padding-bottom: constant(safe-area-inset-bottom);
  padding-bottom: env(safe-area-inset-bottom);
  background: rgba(253, 251, 247, 0.98);
  border-top: 2rpx solid #F3EBDD;
  box-shadow: 0 -8rpx 24rpx rgba(41, 37, 36, 0.06);
  display: flex;
  align-items: flex-start;
  z-index: 100;
}
